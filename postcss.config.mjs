const config = {
  theme: {
    extend: {
      fontFamily: {
        butler: ['Butler', 'serif'],
        script: ['Script', 'cursive'],
      },
    },
  },
  plugins: ["@tailwindcss/postcss"],
};

export default config;
